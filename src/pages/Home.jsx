import ArticlesList from 'components/ArticlesList';
import Section from 'components/Section';

const Home = () => {
  return (
    <Section>
      <ArticlesList />
    </Section>
  );
};

export default Home;
